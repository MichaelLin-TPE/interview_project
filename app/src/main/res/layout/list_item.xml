<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tool="http://schemas.android.com/tools">

    <data>
        <variable
            name="vm"
            type="com.interview.interviewproject.list.ListItemViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:id="@+id/list_item_click_area"
        android:layout_height="wrap_content"
        xmlns:app="http://schemas.android.com/apk/res-auto">

        <ImageView
            android:contentDescription="@null"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:id="@+id/list_item_image"
            android:layout_width="@dimen/list_item_image_default_width"
            android:layout_height="@dimen/list_item_image_default_height"/>

        <TextView
            android:id="@+id/list_item_id"
            tool:text="123"
            android:textSize="@dimen/text_size"
            android:textColor="@color/black"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="40dp"
            android:gravity="center"
            android:layout_width="0dp"
            android:layout_height="wrap_content"/>

        <TextView
            android:id="@+id/list_item_title"
            android:textSize="@dimen/text_size"
            android:textColor="@color/black"
            tool:text="test"
            app:layout_constraintTop_toBottomOf="@id/list_item_id"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:lines="2"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginTop="20dp"
            android:layout_width="0dp"
            android:layout_height="0dp"/>




    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>


